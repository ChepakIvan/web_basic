/* Встановлює шрифт для всього тексту в body як 'serif', що є гарним шрифтом з засічками. */
body {
    font-family: serif;

    /* Задає розмір шрифту для тексту в body на 14 пікселів. */
    font-size: 14px;

    /* Задає висоту рядка тексту в body на 1.25 (125% від розміру шрифту), що поліпшує читабельність. */
    line-height: 1.25;

    /* Задає колір тексту в body на #777 (сірий). */
    color: #777;
}

/* Встановлює стиль шрифту для вкладених списків (ul в ul, ol в ul, ul в ol, ol в ol) як жирний курсив. */
ul ul,
ol ul,
ul ol,
ol ol {
    font-weight: bold;
    font-style: italic;
}

/* Встановлює стилі шрифту для заголовків рівня h1-h6. */
h1, h2, h3, h4, h5, h6 {
    /* Встановлює шрифт для заголовків як 'Cormorant Infant', серіфний шрифт. */
    font-family: 'Cormorant Infant';

    /* Встановлює нормальну жирність шрифту (не жирний). */
    font-weight: normal;

    /* Встановлює нормальний стиль шрифту (не курсив). */
    font-style: normal;

    /* Встановлює відстань між буквами на 0.5px. */
    letter-spacing: 0.5px;

    /* Перетворює текст заголовків на капіталізований вигляд (кожне слово починається з великої літери). */
    text-transform: capitalize;

    /* Задає колір тексту для заголовків на чорний (#000). */
    color: #000;
}

/* Встановлює розмір шрифту для h1 на 32 пікселя (заголовок першого рівня). */
h1 {
    font-size: 32px;
}

/* Встановлює розмір шрифту для h2 на 26 пікселя (заголовок другого рівня). */
h2 {
    font-size: 26px;
}

/* Задає подвійну висоту рядка для елементів з класом .double-line, що поліпшує відстань між рядками тексту. */
.double-line {
    line-height: 2;
}

/* Встановлює стилі для блоку з класом .mail-box, що включають ширину, фон, рамку та відступи. */
.mail-box {
    /* Встановлює ширину блоку .mail-box на 150 пікселів. */
    width: 150px;

    /* Встановлює фон блоку .mail-box на світло-зелений колір (#e3fff6). */
    background-color: #e3fff6;

    /* Додає рамку блоку .mail-box товщиною 5 пікселів та кольору #c2e4c2. */
    border: 5px solid #c2e4c2;

    /* Встановлює внутрішні відступи блоку .mail-box: вертикальний - 5px, горизонтальний - 20px. */
    padding: 5px 20px; /* вертикальні відступи 5px, горизонтальні 20px */
}
/* Встановлює ширину блоку .ruler на 150 пікселів. */
.ruler {
  width: 150px;

  /* Встановлює фон блоку .ruler на зелений колір (lime). */
  background-color: lime;

  /* Вирівнює текст в середину блоку .ruler. */
  text-align: center;
}

/* Встановлює стилі для елементів з класом .email, включаючи обрізання слів і стилізацію першої літери. */
.email {
    /* Обрізає довгі слова в елементах .email, якщо вони не поміщаються в блок. */
    word-wrap: break-word;

    /* Змінює стиль першої літери в елементах .email на окремий блок. */
    display: block;
}

/* Встановлює стилі для абзаців (текстових елементів <p>), включаючи вирівнювання тексту зашифрованого. */
p {
    /* Вирівнює текст в абзаці на ширину, що робить його більш читабельним. */
    text-align: justify;
}

/* Встановлює стилі для блоку з класом .our-adv, включаючи фон, відступи, колір та розмір шрифту. */
.our-adv {
    /* Встановлює фон блоку .our-adv як зображення з URL 'http://picsum.photos/id/357/700/400?blur=5'. */
    background-image: url('http://picsum.photos/id/357/700/400?blur=5');

    /* Встановлює верхній внутрішній відступ блоку .our-adv на 30 пікселів. */
    padding-top: 30px;

    /* Встановлює нижній внутрішній відступ блоку .our-adv на 60 пікселів. */
    padding-bottom: 60px;

    /* Задає колір тексту у блоку .our-adv на білий. */
    color: white;

    /* Встановлює розмір шрифту в блоку .our-adv на 16 пікселів. */
    font-size: 16px;
}

/* Встановлює стилі для блоку з класом .our-adv, включаючи фонове зображення, його розмір та положення. */
.our-adv {
    /* Встановлює фонове зображення блоку .our-adv, як зображення з URL 'http://picsum.photos/id/357/700/400?blur=5'. */
    background-image: url('http://picsum.photos/id/357/700/400?blur=5');

    /* Встановлює розмір фонового зображення так, щоб воно займало весь блок (підігнати розмір). */
    background-size: cover;

    /* Вирівнює фонове зображення по центру блоку .our-adv. */
    background-position: center;
}

/* Встановлює верхній зовнішній відступ для заголовків другого рівня h2 на 60 пікселів. */
h2 {
    margin-top: 60px;

    /* Знімає зовнішній відступ знизу для заголовків другого рівня h2. */
    margin-bottom: 0;
}